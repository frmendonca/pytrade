
from pathlib import Path

PATH_CONFIG_FILES = Path(__file__).parent / "config"
PORTFOLIO_CONFIG_PATH = PATH_CONFIG_FILES / "portfolio.json"
OPTIONS_CONFIG_PATH = PATH_CONFIG_FILES / "options.json"
SIMULATION_CONFIG_PATH = PATH_CONFIG_FILES / "confsim.json"